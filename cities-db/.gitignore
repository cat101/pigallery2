# Intermediate downloads from geonames.org — re-fetched by build-cities-db.js
tmp/

# Generated by `node cities-db/build-cities-db.js` (~15 MB).
# Built at image-build time (Dockerfile) so the prod container ships with it
# without git ever tracking it. Outside Docker, run the script once on
# install.
cities1000.sqlite
